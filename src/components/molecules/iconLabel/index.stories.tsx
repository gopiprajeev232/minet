import { ComponentStory } from "@storybook/react";
import IconLabel from ".";

export default {
    component: IconLabel,
    title: 'Molecules/IconLabel'
}

const Template: ComponentStory<typeof IconLabel> = (args) => <IconLabel />;

export const Story = Template.bind({})